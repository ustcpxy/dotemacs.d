;;; init-org-roam.el --- setup org-roam -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:

(setq org-roam-directory "~/pkms/roam/")
(setq org-roam-db-location (concat org-roam-directory ".org-roam.db"))

(provide 'init-org-roam)
;;; init-org-roam.el ends here
